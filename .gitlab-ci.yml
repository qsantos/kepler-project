image: registry.gitlab.com/qsantos/orbit/ci

test:
  script:
    - cppcheck --suppress=missingIncludeSystem --error-exitcode=1 --quiet --enable=all *.h *.c *.hpp *.cpp
    - make test clean
    - ./test
